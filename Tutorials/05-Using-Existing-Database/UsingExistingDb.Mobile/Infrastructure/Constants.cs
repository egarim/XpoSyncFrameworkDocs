using System;
using System.Collections.Generic;
using System.Text;

namespace UsingExistingDb.Mobile.Infrastructure
{
    public class Constants
    {
        public const string ServerUrlKey = nameof(ServerUrlKey);
        public const string IdentityKey = nameof(IdentityKey);
        public const string XpoSyncConnectionString = nameof(XpoSyncConnectionString);
        public const string SyncDate = nameof(SyncDate);
        public const string DateFormat = "dddd, dd MMMM yyyy HH:mm:ss";
    }
}
